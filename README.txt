# Telegram Expense Bot

Бот для учёта личных расходов с удобным интерфейсом и статистикой.

## ✨ Возможности

### 📊 Учёт расходов
- **Добавление расходов** командой `/add 1500 продукты` или через кнопки
- **Просмотр за сегодня** — `/today` показывает все сегодняшние траты
- **Статистика за месяц** — `/month` аналитика по категориям с графиками

### 🎯 Особенности
- **Персональные данные** — каждый пользователь видит только свои расходы
- **Inline-кнопки** — быстрый выбор категорий без ввода текста
- **Reply-клавиатура** — меню с кнопками для удобной навигации
- **Валидация ввода** — проверка сумм и категорий на корректность
- **Красивое форматирование** — сообщения с эмодзи и HTML-разметкой

### 📁 Категории расходов
- 🛒 Продукты
- 🚗 Транспорт  
- 🎬 Развлечения
- ☕ Кафе
- 🏥 Здоровье
- 👕 Одежда
- 📦 Другое
- *И возможность добавлять свои*

## 🛠 Технологии

- **Python 3.10+**
- **python-telegram-bot 20.0+** — асинхронный фреймворк для Telegram ботов
- **SQLite 3** — лёгкая встраиваемая база данных

## 🚀 Запуск

### 1. Клонирование репозитория
```bash
git clone <ваш-репозиторий>
cd expense_tg_bot


### 2. Создание виртуального окружения
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate

### 3. Установка зависимостей
pip install -r requirements.txt

### 4. Создайте файл .env в корне проекта:
BOT_TOKEN=ваш_токен_от_BotFather

### 5. Запуск
python bot.py

___________________________________________________________

Структура проекта

expense_tg_bot/
├── bot.py                    # Точка входа
├── config/                   # Конфигурация
│   ├── config.py            # Настройки бота
│   ├── texts.py             # Текстовые константы
│   ├── categories.py        # Категории расходов
│   └── currencies.py        # Настройки валют
├── database/                # Работа с БД
│   ├── models.py           # Модели таблиц
│   └── crud.py             # CRUD операции
├── handlers/               # Обработчики
│   ├── command_handlers.py # Команды (/start, /add)
│   ├── message_handlers.py # Обычные сообщения
│   └── callbacks.py        # Inline-кнопки
├── keyboards/              # Клавиатуры
│   ├── reply.py           # Reply-клавиатуры
│   └── inline.py          # Inline-кнопки
├── utils/                  # Вспомогательные функции
│   ├── formatter.py       # Форматирование сообщений
│   └── logger_decorator.py # Декоратор логирования
├── requirements.txt        # Зависимости
├── .env.example           # Пример переменных окружения
└── README.md              # Этот файл