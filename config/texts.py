"""
String constants for the bot.
All user responses are collected here for easy editing.
"""

from dataclasses import dataclass


@dataclass(frozen=True)
class CommandTexts:
    """Bot command texts."""

    START = """
Привет, {first_name}! 👋

Я бот для учёта расходов.
Я помогу вам отслеживать, на что уходят деньги.

📋 <b>Основные команды:</b>
/add <i>[сумма] [категория]</i> — добавить расход
/today — расходы за сегодня
/month — статистика за месяц
/help — показать это сообщение

Просто начните вводить команды!
"""

    HELP = """
🛠 <b>Доступные команды:</b>

<b>Основные</b>
/start — начать работу
/help — эта справка

<b>Учёт расходов</b>
/add <i>сумма категория</i> — добавить расход
/today — показать сегодняшние расходы
/month — статистика за текущий месяц

<b>Настройки</b>
/menu — показать меню с кнопками
/hide — скрыть клавиатуру

Пример: <code>/add 1500 продукты</code>
"""


@dataclass(frozen=True)
class ErrorTexts:
    """Error messages."""

    INVALID_FORMAT = "❌ Неверный формат. Используйте: /add <сумма> <категория>"
    DB_CONNECTION = "⚠️ Ошибка подключения к базе данных. Попробуйте позже."
    NOT_A_NUMBER = "🔢 Сумма должна быть числом!"
    INVALID_NUMBER = "💰 Сумма должна быть от 0.01 до 1 000 000"
    INVALID_CATEGORY = "📛 Категория должна быть от 2 до 50 символов"


@dataclass(frozen=True)
class CallbackTexts:
    """Callback handler texts."""

    CONFIRM_QUESTION = "Добавить {amount} ₽ в категорию '{category}'?"
    SUCCESS_ADDED = "✅ Добавлено: {amount} ₽ ({category})"
    PROMPT_CUSTOM_CATEGORY = "Введите название своей категории:"
    CANCELLED = "❌ Отменено"


@dataclass(frozen=True)
class GeneralTexts:
    """General bot responses."""

    WELCOME_BACK = "С возвращением, {first_name}! 👋"
    UNKNOWN_COMMAND = "🤔 Неизвестная команда. Используйте /help."
    NO_EXPENSES_TODAY = "За сегодня расходов нет."
    NO_EXPENSES_MONTH = "В этом месяце ещё нет расходов."
    CATEGORY_PROMPT = "Выберите категорию для {amount} ₽:"
    EXPENSE_ADDED = "Расход {amount} на '{category}' сохранён!"

    ADD_EXPENSE_PROMPT = """
💰 <b>Введите расход в формате:</b>

<code>1500 продукты</code>
или
<code>750 кафе обед с коллегами</code>
"""

    CATEGORIES_LIST = """
📂 <b>Доступные категории:</b>

🛒 продукты
🍔 еда
🚗 транспорт
🎬 развлечения
☕ кафе
🏥 здоровье

💡 <i>Или создайте свою!</i>
"""


@dataclass(frozen=True)
class ButtonTexts:
    """Button texts."""

    ADD_EXPENSE = "➕ Добавить расход"
    CATEGORIES = "📂 Категории"
    CONFIRM_YES = "✅ Да, добавить"
    CONFIRM_NO = "❌ Нет, отменить"
    CATEGORY_HINT = "💡 <i>Используйте любую категорию при добавлении расхода</i>"
    MENU_TITLE = "Основное меню:"
    CUSTOM_CATEGORY = "✏️ Своя категория"
    CANCEL = "❌ Отмена"


@dataclass(frozen=True)
class BotTexts:
    """All bot texts in structured form."""
    commands = CommandTexts()
    errors = ErrorTexts()
    callbacks = CallbackTexts()
    general = GeneralTexts()
    buttons = ButtonTexts()


texts = BotTexts()
